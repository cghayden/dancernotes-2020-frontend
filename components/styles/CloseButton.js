import styled from "styled-components";

const CloseButton = styled.button`
  background: none;
  /* color: black; */
  border: 0;
  position: absolute;
  z-index: 2;
  right: 0;
`;

export default CloseButton;
