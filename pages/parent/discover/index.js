import ParentNoFilterLayout from '../../../components/Parent/ParentNoFilterLayout'
import Card from '../../../components/styles/Card'
export default function discoverPage() {
  return (
    <ParentNoFilterLayout page={'Discover'}>
      <Card>
        <h3>Coming Soon!</h3>
        <p>Discover retailers and sales near you!</p>
        <p>Find conventions, master classes, auditions, and camps!</p>
        <p>Find a performace to attend!</p>
      </Card>
    </ParentNoFilterLayout>
  )
}
